module.exports = {
    class: `const Router = require('koa-router');
            const controller = require('');

            const router = new Router({});

            $incPoint

            module.exports = router;
            `,
    interface: `router.$interfaceMethod($interfaceUrl, controller.$interfaceName);`,
};